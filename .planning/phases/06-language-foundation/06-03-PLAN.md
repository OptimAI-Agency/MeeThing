---
phase: 06-language-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/Hero.tsx
  - src/components/Features.tsx
  - .planning/phases/06-language-foundation/06-03-AUDIT.md
  - docs/copy-glossary.md
autonomous: true
requirements:
  - COPY-01
must_haves:
  truths:
    - "The landing page (Hero + Features) contains no instance of the locked deprecated term \"Calendar Integration\""
    - "A written audit (06-03-AUDIT.md) lists every user-facing string reviewed across src/components, src/pages, and documents which were replaced, kept, or flagged"
    - "Every borderline string flagged in the audit has been appended to docs/copy-glossary.md under \"Flagged for reviewer judgment\""
    - "A project-wide grep for the six forbidden terms (Dashboard, Calendar Integration, Calendar Settings, Calendar Connections, Connections tab, Sync now, Alerts, Syncing…, Synced toast) returns CLEAN for user-facing code (excluding comments, hook identifiers, and filenames)"
  artifacts:
    - path: "src/components/Hero.tsx"
      provides: "Landing hero with Your Calendar label (D-01)"
      contains: "Your Calendar"
    - path: "src/components/Features.tsx"
      provides: "Features list with Your Calendar label (D-01)"
      contains: "Your Calendar"
    - path: ".planning/phases/06-language-foundation/06-03-AUDIT.md"
      provides: "D-12 full-sweep audit record"
      contains: "## Files reviewed"
    - path: "docs/copy-glossary.md"
      provides: "Updated with flagged borderline items"
      contains: "Flagged for reviewer judgment"
  key_links:
    - from: "src/components/Hero.tsx"
      to: "literal string \"Your Calendar\""
      via: "direct JSX text (no glossary import — this is marketing copy, not shared app copy)"
      pattern: "Your Calendar"
    - from: "06-03-AUDIT.md"
      to: "docs/copy-glossary.md#flagged-for-reviewer-judgment"
      via: "cross-reference"
      pattern: "Flagged for reviewer judgment"
---

<objective>
Complete the D-12 full sweep: audit every user-facing string across `src/components/**` and `src/pages/**` against the glossary, replace direct violations of the locked deprecated vocabulary on the landing page (Hero.tsx, Features.tsx), and record every borderline item in an audit doc plus the glossary's "Flagged for reviewer judgment" section for downstream phases (7–11) to resolve.

Purpose: Fulfill COPY-01's "every existing UI string has been reviewed against it" success criterion and give Phase 7/9 reviewers a concrete list of borderline copy to decide.
Output: Two edited landing components, one new audit file, one updated glossary doc section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-language-foundation/06-CONTEXT.md
@.planning/phases/06-language-foundation/06-01-SUMMARY.md
@src/copy/glossary.ts

<interfaces>
This plan does NOT introduce new glossary keys. It uses direct literal replacements for isolated landing-page instances (Hero.tsx uses "Calendar Integration" as one of three wellness feature cards — not a recurring app label, so inline replacement is appropriate and avoids a glossary entry that would otherwise never be reused).

Any string that COULD be glossary-worthy but has no locked decision MUST be flagged in the audit, NOT replaced with an invented glossary key.
</interfaces>
</context>

<threat_model>
Threats:
- **Scope creep** — A full sweep could rewrite marketing copy, violating D-12's rule that borderline cases are flagged, not rewritten. MITIGATED: this plan only replaces instances of the six named deprecated terms (D-01..D-03, D-05..D-08 forbidden list). Everything else goes to the audit.
- **Silent marketing regression** — Changing "Calendar Integration" → "Your Calendar" on a feature card changes the card's meaning. MITIGATED: accompanying description in Features.tsx is also updated to remain coherent, and change is noted in the audit with before/after snippets.
- **Stale forbidden-terms grep** — Acceptance criterion greps must exclude comments and identifiers; we grep inside JSX string literals only where feasible.
Residual risk: LOW.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace "Calendar Integration" on landing surfaces (Hero.tsx, Features.tsx)</name>
  <files>src/components/Hero.tsx, src/components/Features.tsx</files>
  <read_first>
    - src/components/Hero.tsx
    - src/components/Features.tsx
    - .planning/phases/06-language-foundation/06-CONTEXT.md (D-01)
    - src/copy/glossary.ts (no new keys needed — just read to confirm "Your Calendar" is canonical)
  </read_first>
  <action>
**File A: `src/components/Hero.tsx`**

Replace (around line 37):
```tsx
<h3 className="text-lg font-semibold mb-2">Calendar Integration</h3>
<p className="text-muted-foreground text-center">
  Seamlessly connect with your existing calendar apps
</p>
```
With:
```tsx
<h3 className="text-lg font-semibold mb-2">Your Calendar</h3>
<p className="text-muted-foreground text-center">
  A calmer view of your week, synced from your existing calendar
</p>
```

Rationale (per D-01 + D-04): utility vocabulary out, wellness framing in. This is an isolated marketing instance — no glossary import needed; a single literal replacement keeps the landing page decoupled from the app's COPY module.

**File B: `src/components/Features.tsx`**

Replace (lines 8–14):
```tsx
{
  icon: Calendar,
  title: "Calendar Integration",
  description: "Connect with Google Calendar, Outlook, and other popular calendar applications to automatically analyze your meeting schedule.",
  badge: "Core Feature",
  color: "text-blue-600"
},
```
With:
```tsx
{
  icon: Calendar,
  title: "Your Calendar",
  description: "Connect Google Calendar to see your meetings in a calmer, more human way.",
  badge: "Core",
  color: "text-blue-600"
},
```

RULES:
- Do NOT touch any other feature entry in Features.tsx in this task — the remaining five (Meeting Analysis, Environment Suggestions, Smart Notifications, Role-Based Suggestions, etc.) are v1 marketing copy that has no locked decision and goes to the audit in Task 2.
- Do NOT add a glossary import to either file — these are one-off landing strings and inline literals are correct.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && ! grep -q "Calendar Integration" src/components/Hero.tsx && ! grep -q "Calendar Integration" src/components/Features.tsx && grep -q "Your Calendar" src/components/Hero.tsx && grep -q "Your Calendar" src/components/Features.tsx</automated>
  </verify>
  <done>
- `src/components/Hero.tsx` no longer contains the string `Calendar Integration`
- `src/components/Features.tsx` no longer contains the string `Calendar Integration`
- Both files contain the literal `Your Calendar` at the positions specified
- Hero description updated to wellness framing
- Features first entry updated with calm description and shorter badge
- `npm run lint` exits 0
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
  </done>
  <acceptance_criteria>
    - `! grep -q "Calendar Integration" src/components/Hero.tsx`
    - `! grep -q "Calendar Integration" src/components/Features.tsx`
    - `grep -q '"Your Calendar"' src/components/Features.tsx`
    - `grep -q 'Your Calendar' src/components/Hero.tsx`
    - `grep -q "calmer view of your week" src/components/Hero.tsx`
    - `grep -q "calmer, more human way" src/components/Features.tsx`
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write D-12 full-sweep audit and update glossary "Flagged" section</name>
  <files>.planning/phases/06-language-foundation/06-03-AUDIT.md, docs/copy-glossary.md</files>
  <read_first>
    - src/components/Hero.tsx (after Task 1 edits)
    - src/components/Features.tsx (after Task 1 edits)
    - src/components/Navbar.tsx (if exists — check for dashboard/nav labels)
    - src/pages/Index.tsx
    - src/pages/Login.tsx
    - src/pages/Signup.tsx
    - src/pages/VerifyEmail.tsx
    - src/pages/ForgotPassword.tsx
    - src/pages/ResetPassword.tsx
    - src/pages/NotFound.tsx
    - src/pages/Calendar.tsx
    - src/pages/AuthCallback.tsx
    - src/components/calendar/MeetingsList.tsx
    - src/components/calendar/CalendarSettings.tsx
    - src/components/calendar/settings/BackgroundSettings.tsx
    - src/components/calendar/settings/NotificationSettings.tsx
    - src/components/calendar/settings/SyncSettings.tsx
    - src/components/calendar/settings/WellnessSection.tsx
    - src/components/wellness/BreathingOverlay.tsx (if exists)
    - src/components/wellness/MissedReminderBanner.tsx (if exists)
    - docs/copy-glossary.md
    - .planning/phases/06-language-foundation/06-CONTEXT.md (D-12 instructs full sweep + flagging)
  </read_first>
  <action>
**Step 1 — Grep scan for forbidden terms across full src/ tree:**

Run this command and capture output into the audit doc:
```bash
grep -rnE '(Dashboard|Calendar Integration|Calendar Settings|Calendar Connections|"Sync now"|Syncing…|"Synced"|"Alerts"|Let.{1,3}s get you connected|Your schedule is clear)' src/ 2>/dev/null || echo "CLEAN"
```

After Plans 01/02 and Task 1 of Plan 03 have landed, this grep MUST return exactly `CLEAN` (or only hits that are identifier/comment/filename references, which the audit must explicitly justify).

**Step 2 — Read every file listed in `read_first`** and for each JSX text node or JSX-attribute string (button label, title prop, aria-label, placeholder, toast title/description, heading, paragraph) classify it into exactly one bucket:

- **REPLACED in Plan 01/02/03** — list the key or literal substitution
- **KEPT AS-IS (not in scope)** — identifiers, form labels that are domain terms ("Email", "Password"), third-party product names ("Google Calendar", "Microsoft Outlook")
- **FLAGGED for reviewer judgment** — borderline copy without a locked decision; needs Phase 7/8/9 owner input

**Step 3 — Create `.planning/phases/06-language-foundation/06-03-AUDIT.md`:**

```markdown
# Phase 6 — Plan 03 Full-Sweep Audit

**Completed:** {DATE}
**Scope:** Every `.tsx` file under `src/components/` and `src/pages/` with user-facing text.
**Method:** Manual file read + grep of forbidden vocabulary.

## Forbidden vocabulary grep result

Command:
```
grep -rnE '(Dashboard|Calendar Integration|Calendar Settings|Calendar Connections|"Sync now"|Syncing…|"Synced"|"Alerts"|Let.{1,3}s get you connected|Your schedule is clear)' src/
```

Result: **CLEAN** (or: list any remaining hits with justification — e.g. `src/hooks/useCalendarConnections.ts` uses `calendar-connections` as a query key identifier, NOT user-facing text)

## Files reviewed

| File | Status | Notes |
|------|--------|-------|
| src/components/CalendarHub.tsx | swept in Plan 02 | — |
| src/components/calendar/EmptyStates.tsx | swept in Plan 02 | — |
| src/components/calendar/CalendarConnections.tsx | swept in Plan 02 | — |
| src/components/calendar/settings/SettingsHeader.tsx | swept in Plan 02 | — |
| src/components/Hero.tsx | swept in Plan 03 Task 1 | — |
| src/components/Features.tsx | swept in Plan 03 Task 1 (first card only) | Remaining 5 cards flagged below |
| src/components/calendar/MeetingsList.tsx | {status} | {notes} |
| src/components/calendar/CalendarSettings.tsx | {status} | {notes} |
| src/components/calendar/settings/BackgroundSettings.tsx | {status} | {notes} |
| src/components/calendar/settings/NotificationSettings.tsx | {status} | {notes} |
| src/components/calendar/settings/SyncSettings.tsx | {status} | {notes} |
| src/components/calendar/settings/WellnessSection.tsx | {status} | {notes} |
| src/pages/Index.tsx | {status} | {notes} |
| src/pages/Login.tsx | {status} | {notes} |
| src/pages/Signup.tsx | {status} | {notes} |
| src/pages/VerifyEmail.tsx | {status} | {notes} |
| src/pages/ForgotPassword.tsx | {status} | {notes} |
| src/pages/ResetPassword.tsx | {status} | {notes} |
| src/pages/NotFound.tsx | {status} | {notes} |
| src/pages/Calendar.tsx | {status} | {notes} |
| src/pages/AuthCallback.tsx | {status} | {notes} |

(The executor MUST fill in every {status} and {notes} cell. A valid status is one of: `clean`, `swept`, `has-flags`.)

## Replaced strings

Cross-reference to Plans 01–03. No content owed here — this section is a pointer.

## Flagged for reviewer judgment

These strings were reviewed and left unchanged because they have no locked decision in CONTEXT.md. They are candidates for Phase 7, 8, or 9 when those phases define their copy.

| File | Line | Current string | Why flagged | Suggested phase |
|------|------|----------------|-------------|-----------------|
| src/components/Features.tsx | ~17 | "Meeting Analysis" card title | Marketing positioning; no glossary key | Phase 9 (copywriting) |
| src/components/Features.tsx | ~24 | "Environment Suggestions" | Out-of-scope v2.0 feature; likely removed | Phase 9 |
| src/components/Features.tsx | ~31 | "Smart Notifications" / "Productivity" badge | "Productivity" contradicts calm brand | Phase 9 |
| src/components/Features.tsx | ~38 | "Role-Based Suggestions" | Out-of-scope | Phase 9 |
| src/components/calendar/CalendarConnections.tsx | ~278 | "Events sync on connection" / "Next 7 days of events" / "View in Overview tab" | Utility bullets under success block; "Overview tab" references renamed tab | Phase 7 (today-first layout) |
| src/components/Hero.tsx | ~13 | "Transform Your Meetings with Wellness-Focused Suggestions" | v1 marketing hero; likely rewritten | Phase 9 |
| src/components/Hero.tsx | ~27 | "Start Free Trial" / "Watch Demo" | CTA strings; product has no free trial — stale copy | Phase 9 |

(The executor MUST add to this table any additional borderline strings encountered during Step 2. Minimum: the table above as a seed. Do NOT remove the seed rows.)

## Kept as-is (reference for next reviewer)

| String | File | Why kept |
|--------|------|----------|
| "Google Calendar" | multiple | Third-party product name |
| "Microsoft Outlook" | CalendarConnections.tsx | Third-party product name |
| "Apple Calendar" | CalendarConnections.tsx | Third-party product name |
| "Email" / "Password" | auth pages | Standard form field labels |
| "Cancel" | dialogs | Standard UI verb; already in COPY.disconnect.cancel for disconnect dialog |

## Sign-off

- [ ] Forbidden vocab grep returns CLEAN
- [ ] Every file in read_first has a status row
- [ ] Every borderline string has a flagged row with suggested phase
- [ ] Glossary doc has been updated with the flagged items
```

**Step 4 — Update `docs/copy-glossary.md`:**

Find the existing section heading `## Flagged for reviewer judgment` (created in Plan 01 as empty placeholder) and replace its contents with a table copied from the `06-03-AUDIT.md` Flagged section (same columns). Prepend the line:

> Sourced from Phase 6 Plan 03 full sweep audit — see `.planning/phases/06-language-foundation/06-03-AUDIT.md` for methodology.

Do NOT replace the rest of `docs/copy-glossary.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-language-foundation/06-03-AUDIT.md && grep -q "Files reviewed" .planning/phases/06-language-foundation/06-03-AUDIT.md && grep -q "Flagged for reviewer judgment" .planning/phases/06-language-foundation/06-03-AUDIT.md && grep -q "Phase 6 Plan 03 full sweep" docs/copy-glossary.md && ! grep -rnE '(Dashboard|Calendar Integration|"Sync now"|Syncing…|"Synced"|"Alerts"|Let.{1,3}s get you connected|Your schedule is clear)' src/components src/pages || echo "CLEAN"</automated>
  </verify>
  <done>
- `.planning/phases/06-language-foundation/06-03-AUDIT.md` exists with all required sections
- Audit table has a status row for every file listed in `read_first`
- Audit "Flagged for reviewer judgment" table contains at least the seven seed rows plus any additional borderline strings found
- `docs/copy-glossary.md` "Flagged for reviewer judgment" section has been populated (not left as placeholder) and cites the audit
- Project-wide grep for forbidden terms in `src/components` and `src/pages` returns CLEAN
- `npm run lint` exits 0
- `npm run build` exits 0
  </done>
  <acceptance_criteria>
    - `test -f .planning/phases/06-language-foundation/06-03-AUDIT.md`
    - `grep -q "^# Phase 6 — Plan 03 Full-Sweep Audit" .planning/phases/06-language-foundation/06-03-AUDIT.md`
    - `grep -q "## Forbidden vocabulary grep result" .planning/phases/06-language-foundation/06-03-AUDIT.md`
    - `grep -q "## Files reviewed" .planning/phases/06-language-foundation/06-03-AUDIT.md`
    - `grep -q "## Flagged for reviewer judgment" .planning/phases/06-language-foundation/06-03-AUDIT.md`
    - `grep -q "## Kept as-is" .planning/phases/06-language-foundation/06-03-AUDIT.md`
    - `grep -q "Meeting Analysis" .planning/phases/06-language-foundation/06-03-AUDIT.md` (seed flagged row present)
    - `grep -q "Events sync on connection" .planning/phases/06-language-foundation/06-03-AUDIT.md`
    - `grep -q "Phase 6 Plan 03 full sweep" docs/copy-glossary.md`
    - Project-wide forbidden-term scan (user-facing JSX only):
      - `! grep -rn "Calendar Integration" src/components src/pages`
      - `! grep -rn '"Sync now"' src/components src/pages`
      - `! grep -rn "Syncing…" src/components src/pages`
      - `! grep -rn 'Let.s get you connected' src/components src/pages`
      - `! grep -rn "Your schedule is clear" src/components src/pages`
      - `! grep -rn "Calendar Connections" src/components src/pages`
      - `! grep -rn '"Calendar Settings"' src/components src/pages`
    - `npm run lint` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase-level forbidden-vocabulary scan (run after all three plans in Phase 6 are complete):

```bash
grep -rnE '(Dashboard|"Calendar Integration"|Welcome to Calendar|"Calendar Settings"|"Calendar Connections"|"Sync now"|Syncing…|"Synced"|"Sync failed"|Let.{1,3}s get you connected|Your schedule is clear|Welcome! Let|"All set!")' src/components src/pages \
  && echo "FAIL: deprecated vocabulary still present" \
  || echo "CLEAN"
```

Must print `CLEAN`.

Manual sign-off (see 06-VALIDATION.md):
- Landing page (`/`) renders "Your Calendar" in Hero and Features, not "Calendar Integration"
- Audit doc has been reviewed by the user; flagged items routed to correct downstream phases
</verification>

<success_criteria>
1. `Calendar Integration` is absent from `src/components/Hero.tsx` and `src/components/Features.tsx`
2. `Your Calendar` is present in both files at the replaced positions
3. `.planning/phases/06-language-foundation/06-03-AUDIT.md` exists with every required section populated
4. `docs/copy-glossary.md` "Flagged for reviewer judgment" section is populated from the audit
5. Phase-level forbidden-vocabulary grep returns CLEAN across `src/components` and `src/pages`
6. `npm run lint && npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-language-foundation/06-03-SUMMARY.md` listing:
- Landing-page strings replaced (before/after snippets)
- Total files audited count
- Total borderline items flagged
- Forbidden-term grep final result
- Any deviations from the seed flagged table with justification
</output>
