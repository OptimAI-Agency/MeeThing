---
phase: 04-wellness-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/calendar/CalendarHub.tsx
autonomous: true
requirements: [WEL-01, WEL-02, POL-01, POL-02]
gap_closure: true

must_haves:
  truths:
    - "CalendarHub overview tab loading state shows skeleton cards, not a bare animate-spin spinner"
  artifacts:
    - path: "src/components/calendar/CalendarHub.tsx"
      provides: "Skeleton loading state in overview tab connectionsLoading branch"
      contains: "MeetingCardSkeleton"
  key_links:
    - from: "src/components/calendar/CalendarHub.tsx"
      to: "src/components/calendar/MeetingCardSkeleton.tsx"
      via: "import and render in connectionsLoading branch"
      pattern: "MeetingCardSkeleton"
---

<objective>
Close the single verified gap from 04-VERIFICATION.md: replace the bare blue animate-spin spinner in CalendarHub's overview tab loading branch (lines 145-149) with MeetingCardSkeleton placeholders consistent with the rest of the app.

Purpose: POL-01 requires all data-fetching paths to have explicit loading states with no bare spinners. This is the last remaining bare spinner in the app.
Output: CalendarHub.tsx updated with skeleton loading state in the overview tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wellness-engine/04-VERIFICATION.md

<interfaces>
<!-- Existing MeetingCardSkeleton component to use -->

From src/components/calendar/MeetingCardSkeleton.tsx:
```typescript
import { Skeleton } from "@/components/ui/skeleton";

const MeetingCardSkeleton = () => {
  return (
    <div className="glass-light rounded-2xl p-4 sm:p-6">
      <div className="flex items-start space-x-4">
        <Skeleton className="w-1 h-16 sm:h-20 rounded-full flex-shrink-0" />
        <div className="space-y-2 flex-1 min-w-0">
          <Skeleton className="h-5 w-3/5" />
          <Skeleton className="h-4 w-4/5" />
          <Skeleton className="h-3 w-1/4" />
        </div>
      </div>
    </div>
  );
};

export default MeetingCardSkeleton;
```

From src/components/calendar/CalendarHub.tsx (lines 143-149, the gap):
```tsx
{activeTab === "overview" && (
  <div className="space-y-6">
    {connectionsLoading ? (
      <div className="text-center py-16">
        <div className="w-8 h-8 border-2 border-blue-600 border-t-transparent rounded-full animate-spin mx-auto mb-4" />
        <p className="text-gray-500">Loading...</p>
      </div>
    ) : connectedProviders.length === 0 ? (
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CalendarHub overview tab bare spinner with MeetingCardSkeleton</name>
  <files>src/components/calendar/CalendarHub.tsx</files>
  <read_first>
    - src/components/calendar/CalendarHub.tsx (full file -- understand current structure, locate lines 143-149)
    - src/components/calendar/MeetingCardSkeleton.tsx (component to import)
  </read_first>
  <action>
In `src/components/calendar/CalendarHub.tsx`:

1. Add import at the top with other calendar component imports:
```typescript
import MeetingCardSkeleton from "./MeetingCardSkeleton";
```

2. Replace the `connectionsLoading` branch in the overview tab (lines 145-149). Replace this exact block:
```tsx
<div className="text-center py-16">
  <div className="w-8 h-8 border-2 border-blue-600 border-t-transparent rounded-full animate-spin mx-auto mb-4" />
  <p className="text-gray-500">Loading...</p>
</div>
```

With this skeleton placeholder:
```tsx
<div className="space-y-4">
  <MeetingCardSkeleton />
  <MeetingCardSkeleton />
  <MeetingCardSkeleton />
</div>
```

This matches the same pattern used by MeetingsList.tsx for its loading state (3 skeleton cards in a `space-y-4` container). The `MeetingCardSkeleton` uses `glass-light rounded-2xl` styling which matches the app's glassmorphism design system per D-17.

No other changes to CalendarHub.tsx. Do not touch the overview empty state, the connections tab, or the breathing overlay wiring.
  </action>
  <verify>
    <automated>cd "/Users/jdm/Library/CloudStorage/Dropbox/Personlig [Stash]/Business/A.I. Adventures/Claude/MeeThing" && npm run lint && npm run build && ! grep -n "border-2 border-blue-600 border-t-transparent rounded-full animate-spin" src/components/calendar/CalendarHub.tsx && echo "PASS: bare spinner removed" || echo "FAIL: bare spinner still present"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/calendar/CalendarHub.tsx contains `import MeetingCardSkeleton from "./MeetingCardSkeleton"`
    - src/components/calendar/CalendarHub.tsx contains `<MeetingCardSkeleton />`
    - src/components/calendar/CalendarHub.tsx does NOT contain `border-2 border-blue-600 border-t-transparent rounded-full animate-spin`
    - src/components/calendar/CalendarHub.tsx does NOT contain the string `Loading...` inside the connectionsLoading branch (the bare spinner text is removed)
    - npm run lint exits 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>CalendarHub overview tab loading state shows 3 MeetingCardSkeleton placeholders instead of a bare blue spinner. POL-01 gap is closed -- all data-fetching paths in the app now have skeleton or branded loading states.</done>
</task>

</tasks>

<verification>
- `npm run lint` passes with no errors
- `npm run build` completes successfully
- `grep -n "animate-spin" src/components/calendar/CalendarHub.tsx` returns ONLY the intentional RefreshCw sync button spinner (line ~180), NOT the bare loading spinner
- CalendarHub.tsx imports and uses MeetingCardSkeleton in the connectionsLoading branch
</verification>

<success_criteria>
- POL-01 gap closed: CalendarHub overview tab connectionsLoading branch renders MeetingCardSkeleton placeholders instead of bare animate-spin spinner
- No regression: breathing overlay, missed reminder banner, tab navigation, and all other CalendarHub functionality unchanged
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-wellness-engine/04-03-SUMMARY.md`
</output>
